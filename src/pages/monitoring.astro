---
import BaseLayout from "../layouts/BaseLayout.astro";
import StatusIndicator from "../components/StatusIndicator.jsx";
---

<BaseLayout>
  <div class="min-h-screen bg-gradient-to-br from-slate-900 via-blue-900 to-indigo-900 p-8">
    
    <!-- Header -->
    <div class="text-center mb-12">
      <h1 class="text-5xl md:text-7xl font-black text-white mb-4 bg-gradient-to-r from-blue-400 via-purple-500 to-pink-600 bg-clip-text text-transparent">
        Live Monitoring
      </h1>
      <p class="text-xl text-blue-200">Real-time equipment monitoring and alerts</p>
    </div>

    <!-- Equipment Status Grid -->
    <div class="mb-16">
      <h2 class="text-4xl font-bold text-white mb-8 flex items-center">
        <span class="mr-4">🔍</span>
        Equipment Status
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <StatusIndicator 
          equipmentType="Perlick Chiller"
          equipmentId="perlick-1"
          client:load
        />
        <StatusIndicator 
          equipmentType="Hoshizaki Ice Maker"
          equipmentId="hoshizaki-1"
          client:load
        />
        <StatusIndicator 
          equipmentType="TurboChef Oven"
          equipmentId="turbochef-1"
          client:load
        />
        <StatusIndicator 
          equipmentType="Frosty Soft-Serve"
          equipmentId="frosty-1"
          client:load
        />
      </div>
    </div>

    <!-- System Overview -->
    <div class="mb-16">
      <h2 class="text-4xl font-bold text-white mb-8 flex items-center">
        <span class="mr-4">⚡</span>
        System Overview
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-white/10 backdrop-blur-md border border-white/20 rounded-2xl p-8 text-center hover:bg-white/20 transition-all duration-300 transform hover:scale-105">
          <div class="text-6xl mb-4">📡</div>
          <h3 class="text-2xl font-bold text-white mb-2">Network Status</h3>
          <p class="text-blue-200">All systems connected</p>
        </div>
        <div class="bg-white/10 backdrop-blur-md border border-white/20 rounded-2xl p-8 text-center hover:bg-white/20 transition-all duration-300 transform hover:scale-105">
          <div class="text-6xl mb-4">🔧</div>
          <h3 class="text-2xl font-bold text-white mb-2">Maintenance</h3>
          <p class="text-blue-200">Next service: Feb 15</p>
        </div>
        <div class="bg-white/10 backdrop-blur-md border border-white/20 rounded-2xl p-8 text-center hover:bg-white/20 transition-all duration-300 transform hover:scale-105">
          <div class="text-6xl mb-4">📈</div>
          <h3 class="text-2xl font-bold text-white mb-2">Performance</h3>
          <p class="text-blue-200">94% average uptime</p>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>